local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

-- Xóa Menu cũ nếu có để tránh trùng lặp khi test nhiều lần
local oldGui = LocalPlayer:WaitForChild("PlayerGui"):FindFirstChild("Script_Test_Image_Menu")
if oldGui then oldGui:Destroy() end

-- 1. KHỞI TẠO KHUNG CHỨA GIAO DIỆN TEST
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "Script_Test_Image_Menu"
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

-- 2. KHUNG CHÍNH (Hiển thị ID 134759374241732 làm nền)
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 430, 0, 250) 
MainFrame.Position = UDim2.new(0.5, -215, 0.5, -125)
MainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0) -- Nền đen dự phòng nếu ảnh lỗi
MainFrame.Active = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = MainFrame

-- Ảnh Nền (ID 134759374241732)
local BgImage = Instance.new("ImageLabel")
BgImage.Name = "BgImage"
BgImage.Size = UDim2.new(1, 0, 1, 0)
BgImage.BackgroundTransparency = 1
BgImage.Image = "rbxassetid://134759374241732"
BgImage.ScaleType = Enum.ScaleType.Crop
BgImage.ZIndex = 1
BgImage.Parent = MainFrame

local BgCorner = Instance.new("UICorner")
BgCorner.CornerRadius = UDim.new(0, 12)
BgCorner.Parent = BgImage

-- Tiêu đề Text hiển thị thông tin
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 30)
Title.Position = UDim2.new(0, 0, 0, 5)
Title.Text = "[ SCRIPT TEST HÌNH ẢNH ]"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 16
Title.Font = Enum.Font.SourceSansBold
Title.BackgroundTransparency = 1
Title.ZIndex = 2
Title.Parent = MainFrame

-- 3. KHUNG HIỂN THỊ ID 130963996438547 (Nằm bên trái)
local TestImage1 = Instance.new("ImageLabel")
TestImage1.Name = "TestImage1"
TestImage1.Size = UDim2.new(0, 100, 0, 100)
TestImage1.Position = UDim2.new(0, 40, 0, 50)
TestImage1.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
TestImage1.Image = "rbxassetid://130963996438547"
TestImage1.ZIndex = 2
TestImage1.Parent = MainFrame

local Label1 = Instance.new("TextLabel")
Label1.Size = UDim2.new(0, 100, 0, 20)
Label1.Position = UDim2.new(0, 40, 0, 155)
Label1.Text = "ID: ...38547"
Label1.TextColor3 = Color3.fromRGB(255, 255, 255)
Label1.TextSize = 12
Label1.BackgroundTransparency = 1
Label1.ZIndex = 2
Label1.Parent = MainFrame

-- 4. KHUNG HIỂN THỊ ID 92781042692285 (Nằm bên phải)
local TestImage2 = Instance.new("ImageLabel")
TestImage2.Name = "TestImage2"
TestImage2.Size = UDim2.new(0, 100, 0, 100)
TestImage2.Position = UDim2.new(1, -140, 0, 50)
TestImage2.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
TestImage2.Image = "rbxassetid://92781042692285"
TestImage2.ZIndex = 2
TestImage2.Parent = MainFrame

local Label2 = Instance.new("TextLabel")
Label2.Size = UDim2.new(0, 100, 0, 20)
Label2.Position = UDim2.new(1, -140, 0, 155)
Label2.Text = "ID: ...2285"
Label2.TextColor3 = Color3.fromRGB(255, 255, 255)
Label2.TextSize = 12
Label2.BackgroundTransparency = 1
Label2.ZIndex = 2
Label2.Parent = MainFrame

-- Nút bấm đóng Test Menu nhanh
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 120, 0, 30)
CloseBtn.Position = UDim2.new(0.5, -60, 1, -45)
CloseBtn.BackgroundColor3 = Color3.fromRGB(255, 65, 65)
CloseBtn.Text = "Tắt Test Menu"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Font = Enum.Font.SourceSansBold
CloseBtn.TextSize = 14
CloseBtn.ZIndex = 3
CloseBtn.Parent = MainFrame

local BtnCorner = Instance.new("UICorner")
BtnCorner.CornerRadius = UDim.new(0, 6)
BtnCorner.Parent = CloseBtn

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- Hỗ trợ kéo menu di chuyển trên màn hình để kiểm tra độ mượt
local dragging, dragInput, dragStart, startPos
local function update(input)
    local delta = input.Position - dragStart
    MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end
MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true; dragStart = input.Position; startPos = MainFrame.Position
        input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end)
    end
end)
MainFrame.InputChanged:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end end)
game:GetService("UserInputService").InputChanged:Connect(function(input) if input == dragInput and dragging then update(input) end end)
